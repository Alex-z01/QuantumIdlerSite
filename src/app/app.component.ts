/* 
This is the TypeScript file for the main app component (AppComponent).
It defines the logic and behavior of the component. This includes handling user interactions, managing data, and interacting with services or APIs.
The component class is typically decorated with @Component, which provides metadata about the component, such as its selector, template, and styles.
*/

import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {

}
